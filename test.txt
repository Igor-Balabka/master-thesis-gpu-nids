========================================================================================
PHASE 1 : SETUP & INITIALIZATION (Exécuté UNE SEULE FOIS au démarrage)
========================================================================================

  [ Ruleset / Signatures NIDS ]
               │
               ▼
  ┌─────────────────────────┐
  │  CPU Rule Compiler      │  --> Construction du graphe Aho-Corasick & Aplatissement
  └────────────┬────────────┘      en Matrice 2D (State x 256)
               │
               │  Transfert H2D Unique (cudaMemcpy)
               ▼
  ┌─────────────────────────┐
  │  GPU VRAM (Global Mem)  │  --> L'automate reste RÉSIDENT EN VRAM pendant toute
  │  [Aho-Corasick DFA]     │      la durée d'exécution du système.
  └─────────────────────────┘

========================================================================================
PHASE 2 : RUNTIME PIPELINE (Boucle Asynchrone Continue à Haut Débit)
========================================================================================

  [ PCAP Trace / Traffic ]
               │
               ▼
  ┌─────────────────────────┐
  │  DPDK EAL (--vdev)      │  --> Ingestion Kernel-Bypass dans les Hugepages RAM (Pinned)
  └────────────┬────────────┘
               │
               ▼
  ┌─────────────────────────┐
  │  CPU Worker (Batching)  │  --> Isolation des Payloads & Création des Batches de paquets
  └────────────┬────────────┘
               │
               │  CUDA Stream 0 / Stream 1 / Stream 2 (Chevauchement H2D / Exec / D2H)
               ▼
  ┌─────────────────────────┐      ┌─────────────────────────┐
  │  Host-to-Device (H2D)   │ ───> │  GPU VRAM (Batch Buffer)│
  └─────────────────────────┘      └────────────┬────────────┘
                                                │
                                                ▼
                                   ┌─────────────────────────┐
                                   │  GPU CUDA Kernels       │ <--- Lit l'Automate DFA
                                   │  (1 Thread / Paquet)    │      déjà présent en VRAM
                                   └────────────┬────────────┘
                                                │
                                                ▼
  ┌─────────────────────────┐      ┌─────────────────────────┐
  │  Device-to-Host (D2H)   │ <─── │  GPU VRAM (Match Vector)│
  └────────────┬────────────┘      └─────────────────────────┘
               │
               ▼
  ┌─────────────────────────┐
  │  CPU Match Aggregation  │  --> Traitement des résultats & Alertes
  └─────────────────────────┘